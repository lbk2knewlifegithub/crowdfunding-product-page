import { createAction } from '@ngrx/store';

export const openTopNavMobile = createAction('[Layout] Open Top Nav Mobile');
export const closeTopNavMobile = createAction('[Layout] Close Top Nav Mobile');
